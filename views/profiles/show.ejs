<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main class="container mt-5">
  <div class="row">
    <div class="col-md-4">
      <img src="<%= profile.avatar %>" alt="<%= profile.name %>'s photo" class="img-fluid rounded-circle">
    </div>
    <div class="col-md-8">
      <h4><%= profile.name %></h4>
      <h1>yooooo <%= isUser ? "you" : profile.name %>!</h1>
      <p class="text-muted">Member since: <%= profile.createdAt %></p>
    </div>
  </div>
</main>

<%- include('../partials/footer') %>
